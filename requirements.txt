# Одна зависимость. Генератор считает на стандартной библиотеке: random со
# своим состоянием дает воспроизводимость по seed, а numpy пришлось бы
# объяснять там, где объяснять нечего.
psycopg[binary]>=3.2
