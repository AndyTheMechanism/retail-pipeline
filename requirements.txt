# One dependency. The generator does its arithmetic on the standard library:
# random with its own state gives reproducibility from a seed, and numpy would
# have to be explained where there is nothing to explain.
psycopg[binary]>=3.2
