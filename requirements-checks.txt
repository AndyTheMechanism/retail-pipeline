# Окружение сверки, отдельное по той же причине, что и у dbt.
#
# Pandas не нужен ни генератору, ни моделям. В requirements.txt генератора
# намеренно ровно одна зависимость - там даже numpy не заводили, потому что
# объяснять его было бы нечем, - и тащить туда pandas ради разовой проверки
# значит ломать это решение задним числом.
pandas>=2.2
psycopg[binary]>=3.2
