# The reconciliation environment, separate for the same reason as dbt's.
#
# Pandas is needed by neither the generator nor the models. The generator's
# requirements.txt deliberately holds exactly one dependency — not even numpy
# got in, because there would have been no way to justify it — and dragging
# pandas in for a one-off check would undo that decision after the fact.
pandas>=2.2
psycopg[binary]>=3.2
