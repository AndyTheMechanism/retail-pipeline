-- Справочник магазинов.
--
-- Единственный источник даты открытия, а на ней стоит зерно обеих витрин: до
-- открытия у магазина нет ни заказов, ни трафика, и это честная пустота, а не
-- пропущенная партиция. Отличают одно от другого дальше по графу: дни до
-- открытия срезает int_store_day_spine, а пустоту, которая осталась после
-- среза, помечает mart_store_daily_quality.

with source as (
    select * from {{ source('raw', 'stores') }}
)

select
    store_id,
    store_code,
    city,
    store_format,
    opened_on
from source
